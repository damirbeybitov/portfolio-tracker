# NOTE: versions for packages Airflow's own constraints file already pins
# (psycopg2-binary, httpx) are intentionally left unpinned here — the
# --constraint flag in the Dockerfile RUN command resolves those to the
# exact versions Airflow 2.9.3 was tested against.
psycopg2-binary
httpx
# Pin yfinance to a known-stable version that handles MultiIndex correctly
# and supports set_tz_cache_location()
yfinance==0.2.44
pandas>=2.0.0